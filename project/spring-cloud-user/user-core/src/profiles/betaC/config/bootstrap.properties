#spring
spring.profiles.active=betaC,db,dubbo,kafka,redis
#eureka
eureka.client.serviceUrl.defaultZone=http://dev.project.cloud.local:8101/eureka
eureka.client.registry-fetch-interval-seconds=5

eureka.instance.leaseRenewalIntervalInSeconds=10
eureka.instance.leaseExpirationDurationInSeconds=20
#log
log.home=logs
log.app=${spring.application.name}