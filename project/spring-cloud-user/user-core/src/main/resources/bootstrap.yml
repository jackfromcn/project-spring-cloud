spring:
  application:
    name: user
  cloud:
    config:
      name: ${spring.application.name}
      label: master
      fail-fast: true
      discovery:
        enabled: true
        service-id: config-center